import { ListPageSkeleton } from "@/components/ui/page-skeletons";

/** The Institutions directory arrives as tiles over a table. */
export default function Loading() {
  return <ListPageSkeleton />;
}
