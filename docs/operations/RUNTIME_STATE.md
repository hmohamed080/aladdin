# Runtime State

<!-- CANONICAL PROJECT MEMORY — mutable current-state snapshot. Refresh at the end of every substantive session. -->

This is a **mutable snapshot** of the current live repository state — not an append-only history (that is [`AGENT_WORK_LOG.md`](./AGENT_WORK_LOG.md)). Overwrite it each session with verified values.

| | |
|---|---|
| **Version** | Runtime snapshot · 2026-09-08 (Hana Showroom Owner dashboard milestone, `feat/hana-showroom-dashboard`, Foundation ticket completion pass done, #46–#51 not started) |
| **Owner** | Foundation / Operations |
| **Last updated** | 2026-09-08 |
| **Updated by** | Claude — Phase 1C/1D closeout (PR #43 merged, single migration deployed, live Hana revalidation), the Showroom Owner dashboard milestone's Foundation increment, and its completion pass (editable bilingual org/branch identity + configurable timezone hierarchy) |
| **Current focus** | **Hana Mansour's account-acceptance effort (Phase 1A–1D) is closed: PR #43 merged to `main` @ `b555142` (parents `27b8943`/`f7ecac1`), hosted Supabase pushed to 57/57 migration parity with the branch-aware `sales_assignable_members` RPC, and a live sign-in as Hana confirmed every Phase 1A finding fixed in production. Verdict: HANA ACCEPTED (technical).** Work has now moved to the next item in the same account-review thread: the [Hana Showroom Owner dashboard milestone](https://github.com/hmohamed080/aladdin/issues/44) — a wayfinder map charted 2026-09-08 with 7 child tickets (Foundation → 5 parallel section tickets → cross-cutting validation/PR), tracked as GitHub issues #44–#51. Branch `feat/hana-showroom-dashboard`, created off `b555142`. The **Foundation ticket** ([#45](https://github.com/hmohamed080/aladdin/issues/45)) is in progress this session: a new `dashboard-period.ts` vocabulary (7d/30d/90d/this-month/this-quarter/custom, Cairo-timezone-correct via `Intl`), an additive bilingual-name migration (`organizations.name_ar/name_en`, `branches.name_ar/name_en/address_ar/address_en` — nullable, no write RPC yet for either table, matching the pre-existing lack of one), a new `ShowroomDashboard` composition (sibling to `BuyerDashboard`/`SupplyDashboard`, routed for `org_type = showroom_dealer`) with the six approved primary KPIs plus a new show-more/less primitive, and header fixes (branch icon, no org-name truncation on desktop). A same-session **completion pass** on ticket #45 then closed the two gaps that first pass had deliberately left as debt: `organization_update_i18n`/`branch_update_i18n` (new SECURITY DEFINER RPCs, capability-gated, no table grants reopened) give owners/managers a real write path for the bilingual columns via a new edit surface on the existing `/b2b/settings` page, and `organizations.timezone`/`branches.timezone` (nullable IANA identifiers, validated by a shared trigger) let the dashboard resolve branch → org → `Africa/Cairo` instead of hardcoding Cairo — `dashboard-period.ts`'s functions now take a `timezone` parameter throughout. Full validation green: 55/55 pgTAP files (2098 tests) after the Foundation-only pass, 114/114 Vitest files (1399 tests) after the completion pass, clean typecheck/lint/build, one full staging-seed rehearsal (`scripts/rehearse_staging_seed.py`) passing clean. One pre-existing, unrelated finding surfaced while running the Python staging-tool suite: `scripts/test_verify_staging_seed.py` has 4 failing tests ("B5 email: 2 account(s) have no primary email contact row") — confirmed unrelated to this work (nothing in `scripts/`/`supabase/staging/`/seed files was touched) and flagged as a separate follow-up task rather than fixed here. Hana's account remains **Technical implementation complete for Phase 1D / Product Owner acceptance still pending a live review** — this dashboard milestone is additional scope layered on the same account, not yet re-reviewed live. **Superseded, prior focus (2026-09-07):** **Staging readiness (item 1) is done; non-admin account review (item 2) is underway with Hana Mansour, the first account.** Phase 1A audited her account end-to-end (code-derived route inventory, capability/RLS review, live sign-in walkthrough) and returned **CONDITIONALLY ACCEPTED**: every journey worked and tenant isolation was clean, but found two real code defects (a raw internal membership id leaking into Customers/Leads instead of the assigned salesperson's name; the header theme toggle's label reading the opposite of the applied theme) and a documentation/hosted-reality drift (the demo manifest's "12/10/7/2" commerce totals and "4-person team with a pending invitation" claim do not match what hosted staging — carrying the permanent `demo-enrichment.sql` Sara Nabil chain — actually shows). Phase 1B implements the approved fixes: `listOrgMembers`/`memberNameMap` (`server/queries/sales.ts`) now resolve names through the trusted `org_members_list` RPC instead of a direct `profiles` join that RLS silently blocked for every teammate but the caller, with a safe fallback (never a raw id) when a caller can't resolve one; `ThemeSwitch`'s label is now action-phrased ("Switch to light/dark theme") through the canonical i18n system instead of the ambiguous "Theme: X" pairing; and the demo-account documentation/generator now explicitly distinguishes the local seed baseline (12/10/7/2) from the permanent hosted-enriched totals (14/12/8/3), corrects Hana's team to 2 active members + 1 pending **join request** (not an invitation), names all three quotations currently on her desk, and records the resolved F6 RFQ-count discrepancy (a research-inventory miss, not a hosted anomaly). See [`docs/operations/AGENT_WORK_LOG.md`](AGENT_WORK_LOG.md) for both sessions' full entries. **Next: a final live revalidation of Hana's account after this PR merges**, then continue non-admin account review with the remaining accounts before cross-account journeys (item 3) — see the *Staging-to-launch completion order* in [`PRODUCT_DIRECTION_GUIDE.md`](../product/PRODUCT_DIRECTION_GUIDE.md). |
| **Superseded "current focus"** | **Staging readiness reconciled; PR #40 and PR #41 merged to `main`** (2026-09-07, Phase 0E, `chore/staging-readiness-phase0`, subsequently merged as PR #42 @ `27b8943`). Phase 0 found hosted Supabase `aladdin-staging` at 35/56 migrations against a `main` expecting all 56 and closed that gap (21 migrations inspected, rehearsed, snapshotted privately, pushed, verified); Phase 0E then reconciled the documentation and verification tooling those phases exposed as stale. Full detail in `AGENT_WORK_LOG.md`. &nbsp;&nbsp;— *Prior context:* **Installer/Technician FINAL MILESTONE GATE (2026-09-05, `feature/installer-pilot`):** every gate the Pilot's own protocol required ran clean on that branch — a from-clean `supabase db reset` (**56 migrations** as of this repository's current commit; the gate itself ran against that branch's own migration count, recorded loosely below as it was originally written), full pgTAP (**2016/2016**, 52 files), `db lint` (public: 3 pre-existing warnings, none new; app: clean), a regenerated `database.types.ts` (byte-identical to committed), the professional-asset Storage security boundary (anon list/GET/HEAD/PUT/DELETE all denied on both buckets; the real `/p/media/<itemId>` proxy correctly scoped), a disposable end-to-end Installer journey with 9 cross-user/authority negative checks, the 56-state responsive/locale/theme matrix (0 flagged), the `aladdin/ui-foundation` semantic-utility guard (11/11), and full frontend validation (Vitest 105 files/1324 tests, `tsc --noEmit`, `next build`, all clean). **This milestone subsequently merged as PR #40** — the gate's own detail is preserved below for the record, but its "uncommitted, awaiting commit review" framing is stale; treat the *Current focus* line above as authoritative for what has and has not merged. &nbsp;&nbsp;— *Prior context, Points Core:* **Points Core — canonical specification (2026-08-30, `feature/points-core`):** the Points contract is settled in [`docs/database/points-core.md`](../database/points-core.md) **before any schema exists**, satisfying `supabase/AGENTS.md` (*no production tables without an approved database specification*). **This increment is specification only — no migration, table, RPC, RLS policy, seed row, Realtime wiring or frontend change; `/b2b/points` stays the shell it is today.** Settled: Points are an **append-only, USER-level ledger** (the shipped route and an asserted nav test already call them *"the caller's own standing, not an organization record"*); `organization_id` is **context, never authority**, and never reaches a `USING` clause; balance is **derived** (`SUM(points_delta)`), never a writable column; entries are immutable (`app.forbid_mutation()` triggers, as `audit_log` uses) with **no client INSERT/UPDATE/DELETE** — issuance only from inside existing commerce transitions via an internal `SECURITY DEFINER` helper, beside the `app.record_audit_event` call already there; idempotency is the **deterministic** tuple `(user_id, event_type, source_type, source_id)` behind a unique index, not a client token; corrections are **compensating entries**, and points **stay with the person** across every organization change. Colleagues, managers and owners read **nothing**; platform `support`/`administrator` read-only — a deliberate, documented divergence from the Notifications precedent, because a correction cannot be issued by someone who cannot see it. **Only one earning event has product authority** (`referral.organization_approved`, from the direction guide's write-once referral provenance *"so a future rewards feature can credit the salesperson"*); commerce candidates are Tier B, unapproved, each carrying a documented collusion risk. **No numeric point value exists anywhere in the repository and none was invented** — every amount reads PRODUCT DECISION REQUIRED. **Product closed D2 and D6 on 2026-08-30:** the approved earning-event set is **`referral.organization_approved` alone** (no Tier B commerce events — they stay deferred candidates, not flagged-off code), and a corrected balance **displays negative, never clamped or floored**. **Still open (D1, D3–D5):** point values — unresolved **even for the approved event**, so nothing may be wired · manager visibility · expiry · the Sales Score/leaderboard relationship ("Sales Score" is **undefined repo-wide**). **Awaiting product review; not committed, not merged.** &nbsp;&nbsp;— *Prior context, Design system:* **Design system — the approved visual direction is now canonical (2026-08-26, `feature/supplier-dashboard-visual-refresh`, checkpoint `d70ba3b`):** the workspace visual system reviewed live behind `user.email === "fady@example.test"` was approved and is now the Aladdin design system. The gate is gone in every form — the `designLabAtmosphere` prop chain, the `data-design-lab` attribute, the ~400-line scoped override block, and the identity read that fed it (`getPageContext` no longer calls `auth.getUser()`). Approved values moved into `tokens.css` as the tokens themselves: a `--shell` ramp (architectural blue `#24384D`, replacing the `#001537` navy) and a `--workspace-mesh*` / `--workspace-pool-*` atmosphere. `.workspace-frame` and `.workspace-body` now paint NOTHING; `.workspace-atmosphere` is the only ground, viewport-fixed. Header is sticky and translucent, keyed on `header[data-app-header="card"]` so the two bar-variant shells cannot inherit it. Sidebar: control at the top (click = toggle, hover/focus = mode menu), quick access + collapsible groups (`inert` when closed), rounded both-sides active band, fixed Settings/Upgrade. **The design LANGUAGE is global; navigation content and modules are NOT** — supplier modules stay behind `catalog.write`/`catalog.publish`, and the personal `/home` and Admin shells are pixel-identical to before by design. Measured: the approved reference is **0.0000% moved** at the fold in EN-light, AR-light and EN-dark (`design-lab/regress.mjs`). **Open:** the dashboard period selector was pulled from the heading row on review and has no replacement UI — the period still resolves from `?period=`. &nbsp;&nbsp;— *Prior context, Engagement:* **Engagement — Notifications, Chat, `message.sent` (2026-08-23, `feature/engagement-notifications-points-core`):** Notifications Core, Transactional Chat Core, the Chat application/UI layer and the `message.sent` seam are all implemented and green — see [Engagement state](#engagement-state--notifications-chat-messagesent-2026-08-23) below for the model, the migrations and the verification. **Points is not started.** Chat is transactional company correspondence anchored to an rfq/quotation/order, not a messenger: no `/chat` route, no participant list, no attachments, no Realtime. &nbsp;&nbsp;— *Prior context, Sprint 15:* **Supply-side B2B MVP:** one shared SELLING-SIDE workspace now serves Distributor (`org_type` `supplier`), Manufacturer and Importer — not three applications. `commerceStance(orgType)` (`lib/workspace/supply-side.ts`, pure + unit-tested) derives a **presentation default only**: it reorders the SAME nav sections (Overview · Supply · Network · Selling · Sourcing · Business), relabels the SAME commerce routes (`/b2b/rfqs`→Incoming demand, `/b2b/quotations`→Quotations, `/b2b/orders`→Orders) and flips which perspective tab leads. It grants no authority, hides no module and never touches `users`. `OrgContext` gained `orgType` at zero extra reads. **One** `SidebarShell` (three display modes, cookie-persisted, RTL-correct, and **strictly icon-only when compact** — see the 2026-08-18 override) and **one** `CardRail` serve every B2B org type — there is deliberately no per-type sidebar or carousel. New: supply dashboard (sibling to the extracted buyer dashboard; `/b2b/page.tsx` is a thin stance selector), `supplySummary()`, seller Product Management (KPI rail, status tabs, filters, per-product demand), `/b2b/buyers` customer network, `SupplyReport` leading `/b2b/reports`, and an honest roster KPI strip on Team. Reference `UI-UX/references/Distributor/` used for structure only — messages/Reels/invoices/wallet/commissions/carrier tracking/sales-rep targets/Egypt map were **deferred, not faked**, each because no model backs them. `seed-pilot.sql` section 11 gives all three acceptance accounts submitted demand, undecided quotations, in-progress orders, multiple customers and a draft product, using only businesses that already exist. Branch `feature/supply-side-b2b-mvp`; no `.pen` change. **2026-08-19 — supply dashboard visual fidelity.** The dashboard no longer borrows the MODULE page's shape. `Row` (proportional tracks: `lead` 1.55:1, `wide-lead` 2.5:1, `even`, `thirds`) and `Panel fill` replace `WorkPane` **on the dashboard only** — the rfqs/quotations/orders pages still use `WorkPane` and are unchanged. New `features/home/supply-attention.tsx` is the one block no module owns: a cross-stage triage queue (price → chase → order → fulfil, three per stage, six total), drawn as the reference's labelled row cards because four record types carry four different dates and four different verbs. `quotationsWithOrders()` makes "ready for order" exact rather than inferred. The pipeline panel groups all three stages with `PanelRow share` proportion bars (share of its OWN stage) and carries catalogue state in the new `Panel foot`. `QuotationTable compact` drops the counterparty column that duplicates the record cell's meta line. Two measured fixes: the attribute block needed `min-w-0` or money-carrying rows refused to shrink and broke column alignment; and names must CLAMP not truncate, because `text-overflow` clips at the element's own direction and so removed the identifying start of every Latin name in the Arabic workspace. Still no wallet/invoices/Reels/carrier tracking, and still **no comparison-period percentages** — nothing here produces one. Verified in a real browser for all three acceptance accounts at 1280/1440/1600/1920 and one 390px mobile viewport: zero truncation, zero horizontal overflow. |

## Phase & repository

| Field | Value |
|---|---|
| **Current Phase** | **Non-admin account review**, item 2 of the *Staging-to-launch completion order* in [`PRODUCT_DIRECTION_GUIDE.md`](../product/PRODUCT_DIRECTION_GUIDE.md) — first account: **Hana Mansour** (account-acceptance closed; now building her Showroom Owner dashboard) |
| **Current Sprint** | Hana Showroom Owner dashboard milestone — Foundation ticket ([#45](https://github.com/hmohamed080/aladdin/issues/45)) |
| **Current Feature** | Dashboard period vocabulary + Cairo-timezone math, bilingual org/branch name columns, the six primary KPIs, show-more/less primitive, header org/branch distinction fixes |
| **Next Phase** | Work the remaining map tickets ([#46](https://github.com/hmohamed080/aladdin/issues/46)–[#51](https://github.com/hmohamed080/aladdin/issues/51)): needs-attention, product discovery, market movement, purchases/sales summaries, activity log, then cross-cutting validation/visual QA/PR |
| **Current Branch** | `feat/hana-showroom-dashboard` (created from `main` @ `b555142`, which carries PR #43 — the merged Hana Phase 1D work) |
| **Current Milestone** | Hana Showroom Owner dashboard — map [#44](https://github.com/hmohamed080/aladdin/issues/44), Foundation ticket in progress, not yet a PR |
| **Current Remote Repository** | `origin` = `https://github.com/hmohamed080/aladdin.git` |
| **Last Stable Commit** | `b55514261904f673c4cdd2ae9f90aa7d47a6c9c1` — `main`, PR #43 merged (Hana branch-scoping correction; parents `27b8943`/`f7ecac1`) |
| **Last Stable Tag** | `v0.1.0-foundation` (repo `0.1.0`) — created on merged `main` after the closeout PR; Design System stays independently at `1.0.0` |
| **Foundation Release** | Tagged `v0.1.0-foundation` on `main` @ `64e68d6` |
| **Repository Status** | Published to GitHub (`origin`, full history, no squash/force); `main` protection recommended (ADR-0006), not yet applied |
| **Documentation Status** | Updated for Phase 1 — see [`../DOCUMENTATION_STATUS.md`](../DOCUMENTATION_STATUS.md); 0 broken links; no orphan docs |
| **Implementation Status** | **Sprint 6 validated (frontend typecheck/lint/130 tests/build; backend ruff+pytest; Supabase two clean cycles reset+lint+416 pgTAP; 6 two-session races; Playwright E2E executed & green incl. two-context Realtime; executed visual-QA matrix; production perf measured; dev+prod runtime smoke).** Adds post-create **ownership** edits — `set_customer_ownership` (branch/assignee) and `set_lead_source_branch` (source/branch/compatible-reassign), both version/updated_at-guarded, scope+capability enforced, strand-rejecting, transactionally audited; **`customer_type` immutable** (no domain approval); lead lifecycle out of bounds for the lead RPC. Adds **scoped Realtime** (Postgres Changes on `leads`+`follow_up_tasks`; refresh-only client, RLS-scoped, defers on edit, rebuilds on org/branch change). **Sprint 6.1 closeout:** the Realtime filter now narrows to the **active branch** (`branch_id=eq.<branchId>`, matching the list queries) not just the org; a two-context `realtime-scope` E2E proves scope narrowing/teardown/single-channel, sign-out removal, revoked-membership no-leak, open-form deferral, and duplicate→one-row; the visual-QA matrix runs **both roles** full 4×{en,ar}×{light,dark} + dialogs/states (fixed a 42px customer-detail overflow); **Lighthouse was executed** (sign-in Desktop 100/Mobile 98, /b2b 98, /b2b/leads 96 — all targets met); a pre-existing sign-in test flake was made deterministic. Sprint 5.1 merged (PR #8, `5a47011`). Branch `feature/sales-ownership-realtime`, PR #9 to `main` pending. No products/RFQ/quotes/projects/B2C/payments/WhatsApp/OCR/AI. |

## Live engineering state

> **Current override (2026-09-05, `feature/installer-pilot` — FINAL INSTALLER/TECHNICIAN MILESTONE GATE, uncommitted, awaiting commit review):** the destructive/security/matrix gate the Pilot protocol requires before declaring a persona complete, run on top of the Increment 14 composition work below (itself preceded by a visual-composition-correction pass reacting to a populated-build screenshot review: Account Overview cut from a 2000px+ profile-editor scroll to a ~1451px dashboard — header, summary strip, six-module grid, account action area, full stop, everything else moved to `/home/profile/edit`/`/home/settings` rather than being duplicated inline; Reviews moved onto `WorkPane` with a dense continuous `ReviewRow` list and a narrow supporting rail, `mobileOrder="aside-first"` so the summary stacks above the list on a phone; Job Opportunities tightened to `wide:grid-cols-3` with a narrower filter rail; Home's opportunity preview capped at 3 and opened to three columns — all frontend composition only, same authority). **No migration, no schema, no seed, or RLS change this session** — the gate exercises real RPCs/RLS/Storage, it does not modify any of them. **Not committed, not pushed.**
>
> **DB/security gate, from a clean `supabase db reset`:** all 61 migrations applied in order with only expected idempotent-guard NOTICEs; `supabase test db` **2016/2016 pass** (52 files); `supabase db lint --schema public` surfaced 3 pre-existing warnings (`set_customer_ownership` unused variable, `business_save` unused parameter, `job_cancel` never-read variable — all outside this branch's diff, none new), `--schema app` clean; `database.types.ts` regenerated via `supabase gen types typescript --local` after `drop extension pgtap` and is **byte-identical** to the committed file, with zero `tap_funky`/`lives_ok`/`finish`/pgtap-shaped matches. **Storage/public-media security boundary**, tested directly against the Storage REST API (not merely inferred from RLS policy text): anon `POST .../object/list/<bucket>` returns `200 []` (RLS-filtered to nothing) for both `professional-portfolio` and `professional-certificates`; anon unsigned GET/HEAD on a real, published object key returns Storage's `NoSuchKey` (visibility itself denied, not merely download); anon PUT/DELETE on that same key both return 400 and leave the bytes unchanged (confirmed via a service-role re-fetch, same byte count). The **real production access path**, `/p/media/<itemId>` (a byte-proxy — the client never receives a bucket/key/signed URL) was hit directly: 200 with the exact uploaded bytes for the one eligible published portfolio item; 404 (identical response) for a certificate id, a random uuid, that same item once unpublished, and that same item — still flagged public — once its owner's profile was delisted via `set_profile_hidden` (re-listed immediately after). Certificates have no public route at all, confirmed both by the app's own routing and by the public profile page rendering the eligible portfolio item and the one real review while never mentioning a certificate.
>
> **Destructive end-to-end Installer journey**, disposable, real RPCs/actions under RLS actor impersonation (no mocks): real OTP sign-in via Mailpit as `ahmed@example.test` → Home/Settings load, language and theme cookies both verified to actually flip on click → `individual_save_professional` + `user_trades_set` + a direct `profiles.available_for_work` write (there is no RPC — the write path is a narrow column grant, per the component's own doc comment) → a fresh job posted and published by a real org owner → `job_application_submit` → `job_application_withdraw` → re-`job_application_submit` (confirmed same application row id, not a new one) → `job_application_accept` by the poster → assignment appears `scheduled` → `job_assignment_start` → two `job_progress_add` calls to 100% (assignment stays `in_progress` — progress never self-completes) → poster `job_assignment_complete` → poster `job_review_submit` (5★, immutable) → real Portfolio and Certificate items created, real PNG bytes uploaded via the Storage REST API, finalized, the portfolio item published then unpublished (each transition re-verified live against `/p/media`) → Network (`my_network_organizations`) picked up the completed-work organization automatically, no separate write → a `network_referral_create_new` (case-B, genuinely new showroom) referral created **pending** → platform-admin `network_referral_approve` → **+100 points, exactly once** (balance 0→100) → a second `network_referral_approve` call on the same id returned the same organization id with **no second ledger row and no balance change** (idempotent by construction, not merely by convention) → network (completed-work) and referral (introduced) rows verified as two structurally distinct signals, never conflated. **Nine cross-user/negative-authority checks, all refused as designed:** Installer completing their own assignment (`not a member of the posting organization`); a non-poster-org member (Hana/Cairo Ceramics) reviewing the work; the Installer reviewing themselves; another Installer (Ibrahim) finalizing Ahmed's portfolio item (`portfolio item not found` — RLS hides the row rather than naming an owner) or reading his certificate row (0 rows, vs. 1 for the owner); the *poster's own* org member (Dina, with genuine completed work on file) reading that same certificate row (0 rows — completed work is not asset ownership); another user (Ibrahim) writing Ahmed's `available_for_work` (`UPDATE 0`); a well-formed direct `INSERT` into `points_ledger` by an authenticated, non-platform user (`permission denied for table points_ledger` — no client grant exists at all, RLS is not even reached); and a consumer persona calling `job_application_submit` (`a professional account is required to apply for work`). Persona-downgrade write-path hardening was **not independently re-derived live** this pass — it is relied on via the pgTAP suite's existing dedicated coverage (files 09/10/11/37 and others), consistent with "no repository-wide security audit."
>
> **Cleanup:** the portfolio/certificate metadata rows were deleted+purged through their own real owner RPCs and their Storage bytes deleted via the Storage API (both buckets confirmed empty afterward); the pending-then-approved referral row was deleted outright (a plain mutable row); the +100 points award was reversed through `reverse_points_entry` (a compensating entry, `event_invalidated` — balance back to 0, both the original and its reversal kept as permanent ledger history, per the product's own no-delete contract); the professional-profile fields, trade, headline/bio/languages and availability flag were all restored to their exact pre-gate `seed-pilot.sql` values. **Two rows are permanently, structurally unable to be removed, both for the same reason as the prior visual-gate session's fixture — an append-only child table's guard blocks the cascade a delete would need:** the one disposable job this gate drove through real progress + a real completed assignment (its `job_progress_updates` rows are append-only, so the job/assignment/review chain is now real, permanent Pilot history, exactly like the four such jobs the prior session left behind); and the one organization `network_referral_approve` created for the case-B referral (`audit_log.organization_id` cannot be nulled by a delete, since `audit_log` is append-only) — it sits at `pending_verification`, invisible to every discovery surface, harmless.
>
> **56-state responsive/locale/theme matrix** (7 pages — `/home`, `/home/jobs`, `/home/work`, `/home/profile`, `/home/reviews`, `/home/network`, `/home/settings` — × desktop 1440px/mobile 390px × en/ar × light/dark, every one of the 56 combinations, not a sample): automated per-state checks (`document.documentElement` overflow, `dir` vs. expected locale, console/page errors, HTTP status) flagged **0 of 56**. A further 8-state manual visual spot-check across every page, both viewports, both locales and both themes (desktop AR dark Home/Profile, desktop EN light Jobs, desktop AR light Network, mobile EN dark Settings/Reviews, mobile AR light Work, mobile AR dark Jobs) found no giant accidental whitespace, no broken theme token, no stranded mixed-direction text, and a legible mobile bottom bar in every state (the one artifact observed — the bottom nav appearing to overlap a card in a *full-page* screenshot — is a known Playwright `position:fixed` capture quirk, not a live-viewport defect). **Foundation guard:** `src/lint/foundation-guard.test.ts` **11/11**, explicitly pinning that `bg-surface-sunken`/`text-warning-fg`/`border-line`/`text-heading` are still flagged and that the current valid semantic vocabulary (`bg-surface`, `border-accent-solid/40`, `ring-offset-canvas`, gradient utilities, etc.) is not; a full repo-wide `pnpm lint` produced **0 errors** (1 pre-existing, unrelated `react-hooks/exhaustive-deps` warning). **Frontend validation:** Vitest **105 files / 1324 tests**, all pass (one legitimate test update this pass — `foundation.test.ts`'s stored-language-display guard no longer lists `profile-hub.tsx`, matching the composition pass that removed that content from it); `tsc --noEmit` clean; production `next build` clean, all 92 routes. **STOP FOR REVIEW: not committed, not pushed, Full Chat not started, no `.pen` file touched.**

> **Superseded override (2026-09-05, `feature/installer-pilot` — INSTALLER PILOT INCREMENT 14: FINAL INTEGRATION + SETTINGS + REFERENCE COMPLETION):** the last Installer/Technician composition milestone before this gate. Base `main` @ `7e45e28`; prior increment `7e2dd6c` (Increment 13, Network). **No migration, no schema, no seed, no RLS change** — every change is frontend-only (composition, i18n copy, nav, one ESLint rule).
>
> **(1) Settings is a real route** (`/home/settings`), composing only existing authorities: profile-edit entry, availability control, language switch, theme switch, masked sign-in contact + sign-out. No password, 2FA, notification-preference or Chat-settings UI — none of those has a backing model, passwordless included. **(2) Account Overview** (`/home/profile`, reference 04) rebuilt: identity+availability header → a real summary strip (reviews, average rating, completed jobs, points — all real, all reading the SAME query functions their own dedicated pages use) → a six-module grid (My work, Portfolio, My certificates, My reviews, My Points, My network) → a support/account action area (Settings + public-profile link). No fake completion score or verification rating anywhere. **(3) Home** (`/home`, reference 01) rebuilt into an operational dashboard: welcome/status header, current work, a real job-opportunities preview, a professional-snapshot KPI strip, quick access. **(4) Job Opportunities** (`/home/jobs`, reference 02) recomposed only — filters moved into their own rail beside the results; Increment 8's authority (no match %, no distance, no map, no bookmark hearts, no fabricated images) is unchanged. **(5) My Work and Reviews** got a consistency pass only (typography/spacing/header component), no redesign — lifecycle authority untouched. **(6) Network** was NOT redesigned again; its header was swapped onto the same shared `HomeHeader` component every other personal page now opens with, closing the one visual-consistency gap this pass found there.
>
> **(7) The Foundation gap from Increments 11/12** — an unknown semantic Tailwind utility (`bg-surface-sunken`, `text-warning-fg`, …) silently emitting no CSS, with nothing to catch it — is closed: `aladdin/ui-foundation` gained a fourth `no-restricted-syntax` check (`eslint/ui-foundation-guard.mjs`, split out of `eslint.config.mjs` so it stays testable without pulling in `FlatCompat`) with an allow-list transcribed from `tailwind.config.ts` and 11 focused Vitest tests (`src/lint/foundation-guard.test.ts`) pinning both the historical defects it must catch and the false positives found tuning it (`ring-offset-*`, side-plus-width border compounds, `transparent`/`white`/`black`, `bg-gradient-to-*`, a CSS property name inside a `transition-[...]` arbitrary value, and — the sharpest one — a bare `\b` boundary matching "to-" inside an unrelated English sentence). **Running it repo-wide surfaced four real, pre-existing, silently-broken classes, all fixed:** `border-border` (`primitives.tsx`, `global-search.tsx`, `sales/activity-timeline.tsx` — `borderColor` only declares `DEFAULT`/`strong`, never a `border` colour, so this compiled to nothing) and `bg-border` (`supply-blocks.tsx`, now `bg-[var(--border)]`); `border-border-strong` (`stat-tiles.tsx`) and `text-border-strong` (`onboarding/wizard.tsx`, `onboarding/progress-header.tsx`, now `text-fg-muted`) — none of these five files is Installer-pilot code; the guard is repo-wide and so is what it found.
>
> **(8) A second real defect this pass found and fixed: the mobile bottom bar.** `PersonalMobileNav` was built on "a personal account has at most five destinations, so they all fit, no overflow sheet needed" — true when written, false since Points/Reviews/Network arrived and now false again with Settings: a professional's real list is **nine**, rendered as nine equal-width columns on a 375px screen with every label past the first two truncated illegible. Fixed with `personalMobileNavKeys()` (`lib/nav/personal-modules.ts`) — a fixed five-item priority subset (home, jobs, myWork, network, profile) applied only when the full list exceeds five; Points, Reviews, Settings and Add business are not gone, each is a real entry in Account Overview's own grid or Home's Quick access. **(9) A naming collision Account Overview's new grid exposed:** the pre-existing Portfolio hub-module (`profile.portfolio.title`) and the top-level Portfolio-manager page title (`portfolio.title`) were both, independently, "My work" — the exact name the new job-assignments module in the same grid also carries. Renamed to "Portfolio" / "نماذج الأعمال" (reusing the onboarding flow's own existing phrase for the same concept, in both `en.ts` and `ar.ts`), plus its "Manage my work" button to "Manage portfolio" / "إدارة نماذج الأعمال". Neither rename touches any authority — display copy only.
>
> **Validated this session:** frontend `tsc --noEmit` ✓, `pnpm lint` ✓ (0 errors — 1 pre-existing, unrelated `react-hooks/exhaustive-deps` warning on `sidebar-shell.tsx`, confirmed via `git diff` to predate this session), full Vitest **1322/1322** ✓ (105 files), production `next build` ✓ (`/home/settings` present as a real dynamic route). Targeted security/performance source review (route gating, own-data-only queries, no new client writes, no UUID leakage, parallel fetching, no N+1s, no oversized client bundle) found no issues. Live-browser-verified signed in as the seeded `ahmed@example.test` installer: `/home`, `/home/jobs`, `/home/work`, `/home/profile`, `/home/reviews`, `/home/network`, `/home/settings` at desktop (1535px) and mobile (375px), English and Arabic, light and dark — no horizontal overflow, no stranded mixed-direction text found, RTL mirrors correctly including the Network header fix. **Six-reference comparison against `UI-UX/references/Installer-Technician/` completed** for Home, Jobs and Account Overview (line-by-line against 01/02/04); every silhouette/density difference found traces to either an explicitly-forbidden fabrication (match %, distance, bookmark hearts, card photos, learning/training, a manufacturer-brand feed, a hotline, unread-count badges — none has authority) or a deliberate shell/composition choice (this design system's small line-icon `Panel` vocabulary over the reference's bespoke colour illustrations; Settings living in the account action area rather than the module grid, exactly as this increment's own brief specified); My Work, Reviews and Network were reference-corrected in prior increments and only re-verified for consistency here, not re-compared to 03/05/06 from scratch. **Deliberately not run this session** (no schema or RLS change, so not expected to be affected, but not re-verified either): a clean `supabase db reset` + full pgTAP, the Storage API security harness, `db lint`, a fresh `database.types.ts` generation, and the doc-link checker (no dedicated script exists in this repo; the two new/changed docs — this entry and the AGENT_WORK_LOG entry not yet written — carry no new cross-doc links). No destructive end-to-end journey (sign-in → apply → assignment → portfolio/certificates → review → network → account → settings) was run against disposable UAT fixtures this session; the seeded pilot account's own real (empty) state was used for every screenshot instead. **STOP FOR REVIEW: not committed, not pushed, Full Chat not started, no `.pen` file touched.**
>
> **Current override (2026-08-18, `feature/supply-side-b2b-mvp` / PR #34 — PRE-UAT SHELL + SUPPLY-SIDE VISUAL PASS):** one correction pass on the same branch and the same PR. **No migration, no schema, no seed, no RLS, no `.pen` change.** **(1) One shared `AppHeader`** replaces three drifting header bars (B2B · personal `/home` · Admin); surface-specific pieces arrive as slots, so there is no persona clone, and it is deliberately absent from sign-in/sign-up/OTP/onboarding. **No notification bell** — no notification model exists. Language and sign-out moved out of the header row into the account menu on every authenticated surface. **(2) Global search / command palette** (`Ctrl/Cmd+K`): navigation results are LOCAL (same `allowedNavSections` the sidebar draws from, so instant); record results are server-side, debounced 250 ms, request-id guarded, RLS-scoped through the caller-scoped client, pinned to the ACTIVE organization, and capability-gated per entity group. The gate is the pure, unit-tested `lib/search/scope.ts`, asserted against `lib/nav/modules` so search cannot become a back door into a hidden module; platform-admin destinations are gated by a SERVER-resolved role, never a client flag; a personal account issues no record query at all. Bounded at 6 rows/group and a 2-character minimum, every term through `sanitizeSearchTerm`. **(3) Account menu**: identity (display name + the ONE verified contact — the auth model verifies email OR WhatsApp), work context as read-only CONTEXT rather than a second workspace switcher, profile/preferences links, AR/EN, System/Light/Dark, log out. Theme preference gained `system`, resolved by a blocking pre-paint script in `<head>` plus a `ThemeSync` listener — one theme system, not two. **(4) Collapsed sidebar**: the floating hover caption is gone (it painted over page content and raced the expand-on-hover reveal to show the same word twice); hover/focus now lights the icon's own tile, with `aria-label`, active state and the capability-derived module set unchanged. **(5) CardRail**: one arrow click = exactly ONE card, measured from real adjacent-child geometry instead of `cardWidth × cardsPerView` (which jumped to the end on a wide desktop); RTL falls out of the same measurement. **(6) Scrollbars, globally**: stepper arrows removed, dark-mode track/thumb tokenised, Firefox served via `scrollbar-width`/`scrollbar-color` — fenced behind `@supports not selector(::-webkit-scrollbar)` because **Blink IGNORES every `::-webkit-scrollbar-*` rule the moment `scrollbar-width`/`scrollbar-color` matches**, so the obvious "belt and braces" version silently disables the arrow removal. **(7) Supply-side visual fidelity** from `UI-UX/references/Distributor` (structure only, Aladdin tokens throughout, no MARBLEX branding): new shared server-safe `components/ui/workspace-layout.tsx` — `PageHead`, `KpiStrip` (ONE bordered instrument with hairline seams), `WorkPane` (wide working column + narrow context column), `Panel`/`PanelRow`, `NextSteps`, `Band`. `PageHeader` became a thin adapter over `PageHead` and MOVED OUT of the `"use client"` module so pages can pass a component glyph; `StatTiles` gained `layout="strip"`. The supply dashboard was rebuilt to the reference SHAPE (banded head, five KPIs not nine railed tiles, demand queue + status/catalogue context column, performance band, real next-steps row); RFQs/quotations/orders gained a real status donut; Reports moved from the rail to the strip on both the buyer strip and `SupplyReport`. **Still deliberately absent, each because no model backs it:** wallet · invoices/collections · Reels · chat · carrier tracking · maps · quotas · warehouse/ERP · growth badges. **Two real defects found and fixed during validation:** the **language switch never re-rendered the page** (`revalidatePath` clears the server cache but does not pull a fresh RSC payload for the ROOT layout, which owns `<html lang>`/`<html dir>` — the cookie flipped, `dir` flipped imperatively, and every string stayed in the old language; both language controls now reload the document — this is the long-standing `sales.spec` language-switch failure and was **pre-existing**), and **`truncate` on a KPI value is a correctness bug** (`EGP 289,600.00` clipped mid-string renders as a plausible SMALLER number; the strip now wraps and money on a KPI is compact at the caller). Validated this session: frontend typecheck ✓, lint ✓ (0/0), unit **266/266** ✓; new `e2e/global-shell-uat.spec.ts` **16 passed** across chromium-desktop and Pixel 5 (Distributor AR, Manufacturer EN, Importer AR, collapsed-rail hover, one-card rail, header search + account menu, locale switch, personal `/home` palette exposing no business records, Admin gating, non-staff never offered Admin, mobile); regression `supply-side-mvp` + `showroom-mvp` + `showroom-interaction` **122 passed** after four assertions were updated to the intended new behaviour. Full repository E2E, Lighthouse/performance and pgTAP deliberately **not** run (no schema or RLS change). Branch `feature/supply-side-b2b-mvp`; **PR #34 updated, NOT merged.**


> **Current override (2026-08-16, `chore/staging-demo-accounts` — STAGING DEMO-ACCOUNT READINESS, staging-only data + tooling + docs):** base `chore/vercel-services-deploy` @ `44a4cdd`. **No migration was edited, no historical seed file was edited, no RLS policy / grant / security-definer function was touched, no password or auth bypass was introduced, and nothing was pushed or seeded remotely.** The 26 seeded auth users were audited **by impersonation under RLS** (`set_config('request.jwt.claims', …)` with `role = authenticated`, in a rolled-back transaction) against a database holding **only the bundled seeds** — which is how the decisive fact surfaced: **`supabase/demo-seed.sql` is applied to local databases by hand but is NOT in `config.toml [db.seed].sql_paths`, so it is NOT in the staging bundle.** Org A therefore looked populated locally and was empty in the staging shape. **Eleven of 26 accounts had an empty or dead-end experience:** `a-owner` / `a-cairo` / `b-owner` / `sara` returned **zero rows in every module**; `onboarding_progress` and `individual_onboarding` were **empty repo-wide**, so all 14 accounts that land on `/home` opened on a blank profile at ~8% completeness; `a-cairo` held only the superseded `sales.opportunity.*` capabilities while the RLS policy requires `sales.read`, so its nav offered Customers/Leads/Follow-ups over empty lists; and the salesperson affiliation panel was empty for `a-cairo` and `laila`. **All 26 were also unable to sign in at all** — every address is `@example.test`, a reserved TLD (RFC 6761) that cannot receive mail, and the app is **passwordless (Email OTP only)**, so the address *is* the credential path. Fixed with two **staging-only, additive** layers appended inside the existing single transaction and refusal guard: **`supabase/staging/demo-enrichment.sql`** (deliberately absent from `sql_paths`, so `db reset` / pgTAP / Playwright fixtures are unchanged; capabilities are only ever GRANTED to demo memberships exactly as the product's own people-ops UI grants them, and the legacy `sales.opportunity.*` rows are preserved as real history; new rows use a reserved `fa……` UUID prefix; every existing UUID and relationship preserved) and a **generated email remap** driven by **`supabase/staging/demo-accounts.toml`** (26 accounts, **no addresses in git**) plus a **gitignored** `demo-email.toml` the owner supplies. The build **FAILS CLOSED** without a mapping and rejects reserved domains *including subdomains of `example.com`* — a hole a unit test found. `--rehearsal` writes a separately-named artifact so a practice run can never be mistaken for the cloud one. **One account is deliberately left empty:** `nour` resolves to `consent_pending` and is the brand-new-user / pending-invitation journey; the verifier knows her by name. **Also recorded, and not a defect:** `resolveWorkContext` prefers the Personal context whenever a personal persona exists, so **14 of 26 accounts land on `/home`, not `/b2b`** — the same behaviour a previous session identified behind the pre-existing `pilot-landing.spec.ts:65` failure. New tooling: `scripts/staging_demo.py`, `scripts/test_staging_demo.py`, `scripts/rehearse_staging_seed.py`, `supabase/staging/verify-staging-seed.sql` (read-only, wrapped in an always-rollback transaction). **Rehearsed end to end** (`scripts/rehearse_staging_seed.py --isolated`, a throwaway container with the real GoTrue building the `auth` schema and all 28 migrations replayed): first apply loaded **26 users / 26 profiles / 26 contacts / 12 orgs / 13 branches / 17 memberships / 250 capabilities / 14 onboarding_progress / 14 individual_onboarding / 21 products / 21 RFQs / 17 quotations / 12 orders / 7 projects / 9 customers / 10 leads / 10 follow-ups / 11 saved / 6 verifications / 1 invitation / 1 join request / 1 referral / 29 audit rows**; **all 26 accounts verified** for landing route and non-emptiness under RLS (`a-owner` all-zeros → `rfq=1 quo=1 ord=1 prj=1 cust=4 lead=4`; `sara` all-zeros → `rfq=2 quo=2 ord=1 prj=1`); **second apply REFUSED with row counts byte-identical before and after — zero rows written.** Validated this session: **pgTAP 729/729 ✓ (29 files, 0 failures)** on a clean `supabase start` — identical to the Sprint 14 baseline, which is the proof the enrichment stayed out of the local reset path; Python unit **20/20** ✓, frontend typecheck ✓, lint ✓ (0/0), unit **236/236** ✓, `check_doc_links.py` ✓ (**893 links / 106 files / 0 broken**). E2E/Lighthouse/performance gate deliberately not run (no product or schema change). Branch `chore/staging-demo-accounts`; **the seed was NOT executed, Supabase was NOT pushed, and no PR was merged.**
>
> **Superseded override (2026-08-16, `chore/vercel-services-deploy` — VERCEL SERVICES DEPLOYMENT ARCHITECTURE, documentation + config only):** base `main` @ `c1fbad1` (PR #25 merged). **No product feature was added or changed, no schema touched, and nothing was deployed remotely.** **Confirmed runtime architecture: one Vercel project running TWO Vercel Services — `frontend` (Next.js) and `backend` (FastAPI, Python runtime, entrypoint `app/main.py`) — plus Supabase Cloud.** The repository-root **`vercel.json` is now REQUIRED** and is the deployment definition: it declares both service roots and rewrites `/api/backend(/.*)?` → backend, `/(.*)` → frontend, so FastAPI is **same-origin at `/api/backend`** with no absolute base URL to configure per environment. Deploy from the **repository root** — the Vercel Root Directory setting no longer applies and must stay unset. **This supersedes the previous override's three claims** (FastAPI → Railway, FastAPI not required for staging, no `vercel.json` needed). The underlying evidence is unchanged and was re-verified — `frontend/src` still contains no `fetch(` call and `backend/app` still registers exactly one router (`GET /health`) — but under Services, deploying the backend costs one config entry instead of a second platform account, secret store and rollback procedure, so the same facts now favor shipping it. Recorded as **[ADR-0009](../decisions/ADR-0009-vercel-services-deployment.md)**; **ADR-0004 was NOT rewritten** — preserved verbatim with a superseded banner scoping exactly which rows lost force (FastAPI + worker hosting) versus which remain active (Supabase, OpenAI, Azure DI, Sentry, Local→Staging→Production, portability). **Two code facts are load-bearing:** `frontend/src/middleware.ts` pins `export const runtime = "nodejs"` (Vercel Services hosts no Edge Function output; the middleware only does one Supabase auth round trip plus cookie reads/writes, and Node middleware is stable as of Next.js 15.5 — installed 15.5.22), and **`.vercel/` is gitignored** (`vercel link` writes the project link and a pulled OIDC token there). **Two things deliberately left undecided rather than guessed:** the **worker host** — `backend/app/workers/` is interface-only and a persistent queue consumer is a different deployment shape from a request-driven function, so ADR-0009 defers the choice (Vercel Cron/Queues vs. container host) to a new ADR gated on the first real worker, with `backend/Dockerfile` retained as the exit path and its header updated to say so; and **backend `APP_ENV`, which stays UNSET for first staging** — `backend/app/config.py` fails fast at import when `APP_ENV` is `staging`/`production` and any of `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_JWT_SECRET`/`DATABASE_URL` is missing, so setting it today would force provisioning an **RLS-bypassing service-role key to serve a health check**; the runbook binds that flip to the same change that lands the first real endpoint. **The one previously-unverified item is now RESOLVED against the real Preview deployment:** the owner tested PR #28 — `GET /api/health` → 200 (frontend), `GET /api/backend/health` → FastAPI's own `{"detail":"Not Found"}`, which proves the rewrite routes to the backend service, that rewrite order is correct, and that **Vercel Services preserves the original request path** (it does *not* strip the matched prefix). **Fixed by mounting FastAPI under the prefix on one side only:** `app/main.py` declares `API_PREFIX = "/api/backend"` and applies it at its single `include_router(api_v1_router, prefix=API_PREFIX)` call plus the OpenAPI/docs/redoc and `swagger_ui_oauth2_redirect_url` URLs (that last one is not derived from `docs_url` by FastAPI and was the only route left outside the prefix); **`app/api/v1/__init__.py`** — previously empty — is now the single aggregation point for v1 routers so the prefix is structural, not a per-router convention; `health.py` keeps its bare `/health`, and the prefix string appears exactly once in the codebase. **`vercel.json` was NOT changed** (two prefix mechanisms would compete) and **`root_path` was deliberately rejected** (it is for proxies that *strip* the prefix — the opposite of observed behavior). Backend tests grew **10 → 16**, pinning the public contract: `/api/backend/health` 200, bare `/health` 404, doubled prefix 404, every OpenAPI path under the prefix, and no app route anywhere outside it. Validated this session: `pnpm install --frozen-lockfile` ✓, frontend typecheck ✓, lint ✓ (0 errors / 0 warnings), unit **236/236** ✓, production `next build` ✓, backend `ruff check` ✓, backend `pytest` **16/16** ✓ (10 before the routing fix), `scripts/check_doc_links.py` ✓ (**888 links / 105 files / 0 broken**), `vercel.json` valid JSON ✓. **The Node-runtime middleware was verified at runtime, not merely compiled:** the build emits `.next/server/middleware.js` + `.nft.json` with an **empty `middleware-manifest.json`** (the expected Node-runtime artifact shape — Edge middleware would appear *in* the manifest), and a `next start` smoke test reproduces the pre-change behavior exactly — `/api/health` 200, `/auth/sign-in` 200, and `/`·`/b2b`·`/b2b/rfqs`·`/admin`·`/home`·`/onboarding` all 307 to `/auth/sign-in?next=…`. E2E, Lighthouse, pgTAP and the performance gate deliberately **not** run (documentation/config-only change; no schema change). No `.pen` file changed. Branch `chore/vercel-services-deploy`, pushed with a PR to `main` open and **NOT merged**; **nothing deployed manually and no Vercel/Supabase environment variable touched.** This PR **supersedes the two narrower open PRs #26** (`fix/vercel-backend-entrypoint`, the FastAPI entrypoint) **and #27** (`fix/vercel-frontend-node-middleware`, the Node middleware runtime) — it contains both of those changes plus the documentation reconciliation; both were left open deliberately for the owner to close.

> **Superseded override (2026-08-16, `chore/staging-deployment-readiness` — FIRST CLOUD STAGING READINESS, deployment-only):** base `main` @ `944e954` (PR #23 merged). **No product feature was added or changed** and **nothing was deployed remotely** — this pass produced only the repository-side work required before the owner can stand up a cloud STAGING environment. **Confirmed runtime architecture: Vercel (Next.js) + Supabase Cloud, two services.** **FastAPI is NOT REQUIRED for first staging** on three independent pieces of runtime evidence: `frontend/src` contains **no `fetch(` call at all**, `parseServerEnv()` (the only reader of `AI_SERVICE_URL`) is never called outside its own module, and `backend/app` registers exactly one router — `GET /health`. `backend/` was **not refactored, not deleted, and keeps its CI job**; no Render/Railway/Fly configuration was added. **Cloud URL/auth readiness required no code change and none was made:** `frontend/src` has no hardcoded `localhost`, no `NEXT_PUBLIC_SITE_URL`, and no `window.location.origin`; `middleware.ts` derives every redirect from `request.nextUrl.clone()`, so localhost, the Vercel URL and a later custom domain all work unchanged; and `server/actions/auth.ts` is pure OTP (`signInWithOtp` → `verifyOtp`) with **no `emailRedirectTo`, no `/auth/callback` route and no `exchangeCodeForSession`**, so redirect-URL misconfiguration cannot break sign-in. That relocates the one real hosted dependency to the **Magic Link email template**: `config.toml`'s `content_path` is local-only, so a hosted project falls back to Supabase's stock link-only template and the six-digit code never arrives — the runbook makes replacing it a required step, alongside **leaving CAPTCHA off** (no client sends a `captchaToken`). **Env contract:** `publicEnvSchema`/`serverEnvSchema` are now exported and `env.test.ts` **enumerates them** (6 new assertions) so a service-role/secret-shaped name under `NEXT_PUBLIC_*` fails the suite instead of shipping inlined into the client bundle; staging sets only `NEXT_PUBLIC_APP_ENV=staging` + the Supabase URL/anon key, and **neither `SUPABASE_SERVICE_ROLE_KEY` nor `AI_SERVICE_URL` is provisioned**. **Supabase:** all **28 migrations verified to apply in order from an empty database**; no historical migration was edited; remote deployment is `db push` only and `db reset` is documented as local-only. **Staging seed:** the three existing seeds produce exactly the world worth inspecting (Cairo Ceramics Showroom / `hana@example.test`, connected distributors, 16 products, 17 RFQs, 14 quotations, 10 orders, 5 projects, technicians/professionals, sales analytics) but are **not idempotent** (fixed UUIDs, no `ON CONFLICT`, direct `auth.users` inserts) and run only under `db reset`; rather than edit the pgTAP/E2E-pinned files, `scripts/build_staging_seed.py` **generates** a one-time loader from the same `config.toml` `sql_paths` list, in one transaction behind a refusal guard. Rehearsed end-to-end locally: reset `--no-seed` → apply → **26 users / 12 orgs** and the full commerce chain; second apply **refused with zero rows written**; apply to an un-migrated database refused with the "push migrations first" message. **Vercel:** Root Directory `frontend` **with "include files outside root" enabled** (the pnpm lockfile/workspace live at the repo root), Next.js auto-detected, default install/build/output, Node 22, `packageManager: pnpm@9.0.0` — **no `vercel.json` needed and none added**. Validated this session: `pnpm install --frozen-lockfile` ✓, frontend typecheck ✓, lint ✓ (0/0), unit **236/236** ✓, production `next build` ✓ (every route `ƒ` dynamic — the build needs env values to parse but no Supabase connectivity), `next start` smoke ✓ (`/api/health` 200; `/auth/sign-in`·`/auth/sign-up`·`/auth/support` 200; `/`·`/b2b`·`/b2b/rfqs`·`/admin`·`/home`·`/onboarding` all 307 to `/auth/sign-in?next=…`), and targeted authenticated Playwright `pilot-landing` on a production build **6 passed / 2 failed** — the two failures are `pilot-landing.spec.ts:65` (en+ar) and are **pre-existing on `main`, not caused by this branch**: `resolveWorkContext` defaults to the Personal context whenever a personal persona exists, and `youssef@example.test` is seeded with `primary_account_type = 'sales'` **and** an active membership, so he lands `/home` while the spec still expects `/b2b`; this branch's diff touches only `.gitignore`, `frontend/.env.example`, `frontend/src/lib/env/{index,env.test}.ts`, `package.json` and docs — none of the landing/workspace/seed code involved. Repository-wide E2E, Lighthouse, pgTAP and the performance gate deliberately **not** run (deployment-only sprint; no schema change). No `.pen` file changed. Branch `chore/staging-deployment-readiness`, PR to `main` open, **not merged**.

> **Current override (2026-08-18, Sprint 15 — VISUAL UAT FIX ROUND 1):** two global defects from real-browser UAT, both fixed at the shared layer, on the same branch and PR #34. **No schema, migration, RLS, seed, query, business-logic, `CardRail`, scrollbar, global-search or profile-menu change** — the database is untouched and no `.pen` file changed. **(1) Arabic numeral localization.** `lib/ui/format.ts` is now the ONE formatting layer and the only `new Intl.` construction site in `src/`: `formatNumber` / `formatCount` / `formatQuantity` / `formatMoney` / `formatCompactMoney` / `formatPercent` / `formatDate` / `formatDateTime` / `formatTime` / `formatMonth` / `formatRelativeTime`, memoized per (tag, options), with the locale tag spelled **`ar-EG-u-nu-arab`** so the numbering system cannot drift between the server's Node, the browser's ICU and CI (the calendar is pinned to `gregory` for the same reason). Three leak paths closed: the shared primitives that printed raw `number` props (`KpiStrip`, `PageHead`/`PageHeader` count pill, `PanelRow`, `StatTiles`, `TabLinks`, `RankedBars`, `Funnel`, and Admin's `AdminHeader`/`StatTile`/`DistList`) now take a **required** `locale` — required rather than defaulted, so the compiler enumerated all **88 call sites**; `createTranslator` no longer coerces numeric interpolation with `String(val)` but formats a `number` for its bound locale while passing a `string` through verbatim (this was the largest single source — `t("execution.order.itemCount", {count})` rendered `1 عنصر`); and the duplicate formatters in `features/commerce/constants.ts`, `products-table.tsx` and `supply-report.tsx` were removed in favour of re-exports and the shared `orderCountLabel`. **Technical identifiers are the explicit exception** and stay Latin in every locale via `formatIdentifier()` — order/RFQ refs, SKUs, UUIDs, emails, URLs. **(2) Compact sidebar contract.** `sidebar-shell.tsx` gates the mode control's label on the **chosen mode** (`mode === "expanded"`) rather than the panel's momentary width; collapsed and expand-on-hover keep the closed control icon-only through every phase of the reveal, the mode names live only inside the menu it opens, and its accessible name now carries the active mode (`"الشريط الجانبي: مصغّر"`). Nav items were already icon-only with no tooltip; hover/focus lights the icon tile. Validated: typecheck ✓, lint ✓ (0/0), unit **295/295** ✓ (29 new: 20 formatter, 3 translator-interpolation, 6 sidebar). Real-browser UAT via real Email-OTP as `rania@example.test` (Distributor, Arabic — ten routes DOM-scanned, **zero** Latin digits in `#main`), `mahmoud@example.test` (Manufacturer, English — zero Arabic-Indic digits), `hana@example.test` (Showroom — all three modes; reveal floats the panel to 240px while the spacer holds 56px, 17 labels, no duplicates, no `role="tooltip"`, no `title`) and `admin@example.test` (Admin console, both locales). The Admin console keeps its fixed labelled aside — it has no compact mode. Full E2E, pgTAP, performance and the persona matrix were deliberately **not** run, per the brief. **PR #34 updated; NOT merged.**

> **Current override (2026-08-16, Sprint 14 — Showroom INTERACTION REFINEMENT pass):** the final interaction/UI pass before manual UAT, on the same branch and the same PR #23. **No migration, no schema, no seed, no analytics, no architecture and no performance work was redone** — the database stays at **28 migrations** and the acceptance workspace stays **Cairo Ceramics Showroom (`hana@example.test`)** with the seeded connected showroom world intact. Supabase's workspace chrome was used as **interaction reference only**; none of its colors, typography, border system or branding was copied, and the Aladdin tokens, light/dark themes, spacing, type scale, accent behavior and component language are unchanged. **(1) Desktop sidebar display modes.** `components/layout/sidebar-shell.tsx` adds **Expanded · Collapsed · Expand on hover**, chosen from a compact footer control whose menu exposes exactly those three, localized EN/AR. They are three presentations of **one** navigation — `allowedNavSections(capabilities)` still produces the items, so grouping, order, capability filtering, icons, active state and RTL mirroring are identical in all three and a collapsed rail renders exactly as many links as an expanded one; labels survive as accessible names plus a tooltip on hover **and** keyboard focus. The structural decision is a **spacer/panel split**: an outer flex child reserves the RESTING width while an absolutely-positioned inner panel carries the VISUAL width, so a hover reveal floats **inward over the page** and the document never reflows (widening a flex child on hover would relayout the whole document on every pointer pass — the brief's "continuously resizing/shifting the body", and the usual source of hover flicker). `start-0` inside the spacer makes the reveal direction fall out of writing direction: rightward in English, leftward in Arabic, inward in both. The mode is persisted in a **cookie, deliberately not `localStorage`** — it decides a WIDTH, so a value only readable after hydration would paint 15rem then snap to 3.5rem; `AppShell` resolves it server-side and the first HTML byte carries the right width (asserted against the raw response). **No database persistence.** **Mobile is untouched** — the modes are `tablet:`-and-up and the bottom bar + More sheet are unchanged. **(2) Horizontal card rails.** One reusable `components/ui/card-rail.tsx`, **no carousel dependency**: `overflow-x:auto` gives trackpad/wheel/touch, CSS scroll-snap keeps stops on card boundaries, controls appear **only on real overflow**, disable at each end, move by whole cards, scroll smoothly (respecting `prefers-reduced-motion`), and an overflowing rail is keyboard-focusable while one that fits adds no tab stop. **RTL is normalized, not assumed** — `scrollLeft` rests at 0 and travels NEGATIVE in RTL, so every read goes through `Math.abs` and every write flips its sign from the active direction. Applied to exactly three dense peer-card groups: the dashboard KPI strip (eight tiles for a buy-and-sell member), the dashboard action ramp, and the Reports analytics strip — where it also retires a documented defect, since a railed card holds its width instead of shrinking and `EGP 1,103,100.00` no longer truncates at laptop width. **Not converted:** data tables, main report charts, forms, and the large operational lists (catalog grid, saved-products grid, directories). Validated this session: frontend typecheck ✓, lint ✓ (0 errors, 0 warnings), unit **230/230** ✓ (14 new across `card-rail` and `sidebar-shell`), targeted Playwright `showroom-interaction` **15 passed / 0 failed / 0 flaky** (7 declared `isMobile` skips) across chromium-desktop 1440x900 and Pixel 5 and regression `showroom-mvp` **24 passed / 0 failed** across both projects, English and Arabic, plus real-browser UAT as `hana@example.test`. The broad audit, the full-repo integration/performance gate, Lighthouse and pgTAP were deliberately **not** run (no schema change; the performance pass is already accepted). No `.pen` file changed. **PR #23 updated; NOT merged.**

> **Current override (2026-08-15, Sprint 14 — Showroom PRODUCT COMPLETENESS pass):** a depth/usability/analytics pass on top of the module structure described below, on the same branch. **Root cause of the "sparse" showroom found:** `app._organization_public_directory()` filters on `is_verified`, and every pilot organization was seeded unverified (every pilot profile `hidden`) purely to keep global `count(*)` pgTAP assertions frozen — so Distributors, Institutions and Technicians were **structurally empty** for the acceptance account regardless of their UI. Two brittle assertions were rescoped to the record under test (`where id = …`), which proves more and unblocks the seed. **Acceptance workspace moved to the real Showroom/Dealer organization, Cairo Ceramics Showroom (`hana@example.test`)**, in both manual UAT and the e2e suite — Org A is a supplier and Org B a design office, so neither exercised the buyer-first IA. The database now has **28 migrations**: `20260817090001_showroom_purchase_analytics` adds the `order_category_spend` `security_invoker` projection (order value resolved to product category through the quotation lines the order was created from — the only honest link, since an order line is a frozen snapshot with no `product_id`; verified to sum to the order total exactly) and **appends** `requester_branch_id` to `order_list` and `branch_id` + `order_total` to `project_list`. Seeds: `seed-pilot.sql` extended (5 verified counterparties, 7 listed professionals, 12 purchase requests / 10 offers / 7 orders worth **EGP 1,103,100** across six months, 5 projects, 8 shortlisted products, a sell-side chain, and 12 local SVG material swatches under `frontend/public/demo/products`), and the showroom sales book split into **`seed-showroom-sales.sql`** (third entry in `config.toml` `sql_paths`) because the e2e global setup truncates the four sales tables — that setup now re-applies the same file, so `db reset` and e2e agree. User-facing **Supplier → Distributor / المورّد → الموزع** across every surface, applied to message VALUES only (keys, `{supplier}` placeholders, route paths, columns and RPC identifiers are internal and unchanged — **no schema terminology migration**); `showroom_dealer`'s Arabic label moved to "معرض / تاجر" to avoid colliding with the new meaning of موزع. Charts are hand-written **server-rendered inline SVG** (`components/ui/charts.tsx` — trend line, donut, ranked bars, funnel; **no new runtime dependency**), backed by a governed categorical palette `--series-1…6` + `--chart-grid` in `tokens.css` (both themes, every value an existing brand primitive), documented in `UI_UX_SYSTEM_GUIDE.md` under a new **Charts** section. Team no longer renders raw capability keys — `capabilityGroups()` maps them to localized work areas (**display only**; authorization is untouched, guarded by `lib/org/roles.test.ts`). Four real defects found in browser review and fixed: `DataTable` clipped columns wider than their container, money in a `StatTile` overflowed the mobile grid by 23px, `Projects` showed an always-empty branch column on the executing tab, and Arabic mixed numeral systems within one row. Validated this session: frontend typecheck ✓, lint ✓ (0 errors, 0 warnings), unit **216/216** ✓, `supabase db reset` ✓ from clean with all three seeds, pgTAP **729/729** ✓ (5 test files reconciled to the enlarged pilot world), targeted Playwright `showroom-mvp` **16 passed / 1 skipped** on chromium-desktop **and** Pixel 5, plus real-browser UAT across all 13 acceptance routes in English and Arabic. Pre-existing failures confirmed against a stashed baseline and **not** caused by this pass: `shared-onboarding.spec.ts:44`, `business-onboarding.spec.ts:56` (a chain of assertions left stale by an earlier onboarding copy rename; three of them repaired here) and `pilot-uat-round-1.spec.ts:64`. Full-repo E2E, Lighthouse and the performance gate deliberately **not** re-run — the performance pass is already accepted. No `.pen` file changed. **PR #23 updated; NOT merged.**

> **Current override (2026-08-15, Sprint 14 — Showroom MVP Completeness):** `main` is at `678ba32` (PR #22 merged). The database now has **27 migrations** — `20260816090001_showroom_saved_products` adds `public.saved_products` (organization-owned shortlist, PK `(organization_id, product_id)`, RLS scoped to `app.is_org_member`, no direct DML grant), the `save_product` / `unsave_product` security-definer RPCs, the `saved_product_list` `security_invoker` view, and the `persona` column on `app._profile_public_directory()` + `public.profile_public_directory` (a DROP+CREATE, so the full ACL is reasserted — omitting the GRANT made the directory 42501 for every caller, caught by a browser run). Saving is deliberately **not audited**: a private shortlisting act has no lifecycle, counterparty or money attached, so the audit action allow-list is untouched. Validated this session: frontend typecheck ✓, lint ✓ (0 errors, 0 warnings), unit **208/208** ✓ (incl. the rewritten `lib/nav/modules.test.ts` and the bilingual parity gate), `supabase db reset` ✓ from clean with both seeds, pgTAP **729/729** ✓ across 29 files. Targeted Playwright (`showroom-mvp` + `orders-projects`): **21 passed / 0 failed / 0 retries** on desktop (1440×900) **and** Pixel 5, English **and** Arabic — all twelve showroom routes render with a title, no console errors and no horizontal overflow; grouped rail; mobile bar + More sheet reaching every module; perspective tabs; supplier→catalog hand-off; save→shortlist→unsave round trip; RTL flip; and a no-Latin-leak check over the Arabic nav and new headings. **pgTAP must be run from a clean reset** — after a Playwright session, `14_write_path_security_review` #73 and `16_sales_activities_followups` report failures caused by E2E leftovers, not defects. Two `sales.spec.ts` failures (language-switch, branch-limited scope) are **pre-existing on `main`**, confirmed by baseline run. Repository-wide E2E, Lighthouse and the full persona matrix deliberately **not** run — this is not the final Integration Gate. No `.pen` file changed.

> **Current override (2026-08-15, Sprint 13 — Personal Experience + Sales Affiliation + Type Separation):** `main` is at `e7fc5e0` (PR #21 merged). The database now has **26 migrations** — `20260815090001_persona_organization_type_separation` (two disjoint types `public.persona_type` / `public.organization_type` replacing the shared `public.account_type`, which is **dropped**; four columns retyped; `interior_designer`/`engineer` → `design_office` and `contractor` → `contractor_company` for the two organizations whose classification shared a persona spelling; `onboarding_progress.selected_account_type` split into `selected_persona` + `selected_org_type`; every dependent function/view recreated; `apply_account_upgrade` fixed to accept the NULL starting persona Sprint 12 made normal) and `20260815090002_showroom_affiliation` (`organization_join_requests` + `organization_referrals`; `app.membership_grant_sales` as the single trusted affiliation-activation path; showroom directory search over the approved public columns; owner/manager approve+reject under the existing `org.members.manage`; platform referral review that prefers linking over creating; write-once `organizations.source` / `.referred_by_user_id` provenance). Validated this session: frontend typecheck ✓, lint ✓ (0 errors, 0 warnings), unit **204/204** ✓, bilingual parity gate ✓; `supabase db reset` ✓ from clean with both seeds, pgTAP **729/729** ✓ across 29 files (79 new). Targeted production Playwright: **9 passed / 0 failed, no retries** on desktop (all ten required journeys — Consumer home, Engineer home, Salesperson-without-showroom, existing-showroom request + rejection + approval, missing-showroom referral + Admin approval, work-context restoration, persona-never-decides-landing, Arabic RTL, locale switching), and on **mobile (Pixel 5)** the same nine journeys ran with **8 passed / 1 flaky** — journey 4 (existing-showroom request → rejection → approval, four OTP sign-ins) timed out once at the decline step under the slower emulation and passed on retry #1; Playwright labels it flaky rather than hiding it, and the run exits 0. The mobile pass also caught and fixed a real 2px horizontal-overflow defect on both personal homes at 393px. Repository-wide E2E, Lighthouse and the full persona matrix deliberately **not** run — this is not the final Integration Gate. No `.pen` file changed.

Always reflects the current live engineering state of the project. Overwrite each session with verified values.

| Field | Value |
|---|---|
| **Current Sprint** | **Sprint 3 — B2B Sales Domain Foundation** (Phase 2) |
| **Current Epic** | B2B Sales Operating Workflow (customers, leads, pipeline, activities, follow-ups) on the tenant spine |
| **Current Feature** | Tenant-owned sales schema + scope RLS + constrained sales RPCs + audit + dashboard read-models + server-only helpers |
| **Current UI Status** | **B2B sales workspace with edit depth (Sprint 5):** Sprint-4 sign-in + workspace, plus real customer/lead/follow-up **edit** flows (trusted RPCs), richer customer detail (add-activity/add-follow-up/follow-up lists), an accessible confirmation dialog for terminal actions, and a local Playwright E2E foundation. Arabic-first RTL + EN + light/dark; optimistic lead concurrency; localized errors. Wired to real RLS/RPCs (no mock core data). |
| **Current Backend Status** | FastAPI scaffold — `GET /health` only; **no product endpoints**. No backend change in Sprint 3 (sales write paths are Next.js server actions per ADR-0001). |
| **Current Database Status** | **11 migrations** (Sprint 4.2 adds `20260805100000_public_directory_invoker_hardening`); Phase 1 identity/tenancy + Sprint 2/2.1 write paths + **Sprint 3 sales domain** (`customers`, `leads`, `sales_activities`, `follow_up_tasks`; 13 sales workflow RPCs; 5 read-model views; composite-FK tenant safety; scope RLS). All 27 public workflow RPCs are postgres-owned `security definer` (`search_path=""`), executable only by `authenticated`; base tables SELECT-only for client/service roles. **Sprint 4.2:** +2 internal `app._*_public_directory()` definer readers backing the hardened `security_invoker` directory views (migration `20260805100000`). **Sprint 5.1:** `20260805110000_sales_edit_concurrency` adds edit-path optimistic concurrency + explicit `p_clear_*` field-clearing. **Sprint 6:** **13 migrations** — `20260806090001_sales_ownership_and_realtime` adds two ownership RPCs (`set_customer_ownership`, `set_lead_source_branch`; `security definer`, `authenticated`-only, version/updated_at-guarded, strand-rejecting, audited via new actions `customer.reassigned`/`lead.details_changed`) and publishes **exactly** `leads`+`follow_up_tasks` to `supabase_realtime` (RLS-enforced Postgres Changes; no identity/PII table published). **416 pgTAP** assertions across 19 files (two clean cycles). |
| **Current Design System Version** | **1.0.0** (`DESIGN.md` / `design/tokens/*`) |
| **Current Documentation Version** | Technical spec **1.0.0** (Phase 0.7); engineering standards **1.0.0** (Phase 0.8); governance/planning **1.0.0** (Phase 0.9 — ADR-0006, ROADMAP, BACKLOG, TECHNICAL_DEBT, DOCUMENTATION_STATUS, DECISION_LOG); docs index **1.0.0** |
| **Current Deployment Status** | **not deployed — repository-side STAGING readiness complete, awaiting owner cloud setup.** No Vercel/Supabase cloud project is connected yet and there is still **no CD**. The confirmed first-staging runtime is **Vercel + Supabase Cloud only**; **FastAPI is NOT required** (no `fetch(` in `frontend/src`, `parseServerEnv`/`AI_SERVICE_URL` unread, `backend/app` exposes only `GET /health`) and is deliberately not deployed. Owner steps are in [`staging-deployment-runbook.md`](./staging-deployment-runbook.md). A minimum **PR-validation CI** workflow (`.github/workflows/ci.yml`: `frontend`/`backend`/`docs`) is present (must run once, then be selected as required checks in branch protection); repository published to GitHub (`origin`) |

## Git & branch

- **Baseline:** `main` @ `cd7a0ba3c6abf858bd7d55b5a8171630a239cd7b` — PR #41 (agent workflow conventions) merged, on top of PR #40 (Installer/Technician Pilot, `717a156`) merged just before it.
- **Current work branch:** `chore/staging-readiness-phase0` — Phase 0E documentation/tooling reconciliation (this session): `RUNTIME_STATE.md`/`AGENT_WORK_LOG.md`/the staging runbook/the demo-account manifest, the canonical hosted-verifier rewrite, the isolated-rehearsal Storage fast-fail fix, and the staging-to-launch completion order. Branched from `main` @ `cd7a0ba`. **Not yet merged.**
- **Prior branch:** `chore/agent-skills-setup` (merged, PR #41) — the `docs/agents/*` Wayfinder/triage conventions and the `CLAUDE.md` section pointing at them, plus an author-identity fix (a repository-local Git identity that resolved to no GitHub account, causing Vercel to reject the deployment) — see [Migration safety protocol](staging-deployment-runbook.md#migration-safety-protocol).
- **Prior branch:** `feature/installer-pilot` (merged, PR #40) — the Installer/Technician Pilot, FINAL MILESTONE GATE recorded above.
- **Remote:** `origin` → `https://github.com/hmohamed080/aladdin.git` (push preserves full history; no squash, no force). All work merges into `main` via PR; direct pushes to `main` are prohibited.
- **Hosted staging:** Supabase project `aladdin-staging` (ref `kkpkgjrweysgvhvkhzun`) — reconciled to full local/hosted migration parity in Phase 0/0B/0C/0D of this same effort (was 35/56 before). Re-check parity with `supabase migration list --linked` rather than trusting a count recorded here — see [Database & migration state](#database--migration-state) below.

> HEAD moves with each commit; this file trails HEAD by its own commit. Re-derive live values with the [resume commands](#exact-resume-commands) below rather than trusting a pasted hash.

## Architecture state

- **Foundation:** scaffolded and validated. Modular monolith per ADR-0001 (Next.js + Supabase + specialized FastAPI + workers).
- **Python data access:** `supabase-py` (ADR-0005); SQLAlchemy deferred/removed; Alembic excluded.
- **No product features, product tables, or production connections exist.**
- **MVP technical blueprint:** authored under [`../technical/`](../technical/) (Phase 0.7) — system overview, domain model, database design, ERD, storage, RLS, permissions, API contracts, jobs, events, state machines, validation, integrations, future scope. Specification only; open decisions marked `⚑ OPEN`.

## Design system state

- **Version `1.0.0`** — approved, hardened, pre-feature. North star: **"The Aperture."**
- **Authority chain:** `PRODUCT_DIRECTION_GUIDE.md` → `DESIGN.md` → `design/tokens/*.json` → `UI-UX/UI_UX_SYSTEM_GUIDE.md` → `design.pen` → frontend code.
- **Canonical machine tokens:** `design/tokens/{colors,typography,spacing,radii,shadows,motion,breakpoints,z-index}.json` (+ README). Governance: `design/GOVERNANCE.md`; changelog: `design/CHANGELOG.md`; inventory: `design/COMPONENT_INVENTORY.md`; icons: `design/icons/README.md`.
- **Frontend implementation:** `frontend/src/styles/tokens.css` (primitives + light/dark semantics + motion + z-index), `frontend/tailwind.config.ts` (semantic/brand colors, type roles, spacing, radii, shadows, motion, z-index, canonical `tablet`/`desktop`/`wide` screens), `frontend/src/app/globals.css` (base + `prefers-reduced-motion`), `frontend/src/app/layout.tsx` (four self-hosted fonts).
- **The workspace shell is canonical as of 2026-08-26.** `--shell` (#24384D, an architectural blue) is the sidebar's material; `--workspace-mesh*` + `--workspace-pool-*` are the light cool-mineral atmosphere the page is composed on. Two materials, deliberately different lightness, with the atmosphere's seam pool carrying the SIDEBAR's own hue — that one token is what makes the two read as one transition. `UI-UX/UI_UX_SYSTEM_GUIDE.md` § *Workspace Shell & Surface Language* is the policy record.
- **Removed as superseded:** the `--frame*` family and the `--body-veil*` alpha ramp (nothing paints that plane any more), the `frame` Tailwind colour group, the three dead `--navy` *surface* entries (`--navy-air`/`--navy-edge` survive as tint sources only, never surfaces), and the `data-shell-palette="ink"` A/B proposal — **the shell palette is settled and neither candidate won.**
- **`design/tokens/colors.json` has KNOWN PRE-EXISTING DRIFT** (older than this session, now recorded in a `knownDrift` block in the file): it still names Basalt as the dark canvas/surface ramp, while the implementation moved the dark ground to the neutral Carbon ramp several passes ago. The shell/workspace family added on 2026-08-26 IS current; the dark ramp entries are not. For the dark theme, `frontend/src/styles/tokens.css` is ahead of the canonical file. Reconciling it is its own task.
- **Defect fixed in an earlier session:** dark-theme `--primary` referenced an undefined `--lime` → corrected to `--on-dark`.
- **Components implemented:** none yet — inventory entries are `Proposed`/`Draft`. Icon library (Lucide) decided but not installed. PDF/Arabic font strategy is an open item.

## Implemented routes

- **Frontend (Next.js):** `/` (`src/app/page.tsx`) · `/api/health` (`src/app/api/health/route.ts`) — scaffold only.
- **Backend (FastAPI):** `GET /health` (`app/api/v1/health.py`) — scaffold only.

## Auth state

Passwordless model (WhatsApp/Email OTP) is specified; **no auth UI/OTP/session handling wired yet** (explicitly out of Phase 1 scope). The **identity backbone** now exists: a `security definer` trigger `app.handle_new_user()` on `auth.users` bootstraps the canonical `public.users` + `public.profiles` rows atomically (no client-side profile creation; duplicate base identity is structurally impossible). Backend JWT verification for FastAPI is still a later item.

## Database & migration state

**Migration count and hosted parity drift constantly — check them live, never trust a number written in this file:**

```bash
find supabase/migrations -maxdepth 1 -name "*.sql" -type f | wc -l   # local total
supabase migration list --linked                                     # local vs. hosted aladdin-staging, per-migration
supabase db push --linked --dry-run                                  # exact pending list, nothing applied
```

As of Phase 0D of this effort (2026-09-07), hosted `aladdin-staging` was pushed to full parity with the local migration set (was 35/56 before that push closed the gap found in Phase 0). Treat that as a point-in-time fact, not a standing guarantee — re-run the commands above before relying on it.

The bulleted list below is a **historical record of the first 7 migrations only** (Sprint 1–2.1), preserved for what each one specifically added — it has not been extended as the schema grew far past that, and is not a current total.

- **Migrations (first 7, historical):**
  - `20260729000000_extensions.sql` — `pgcrypto`, `pg_trgm`, `vector`/pgvector, `postgis` (in the `extensions` schema).
  - `20260802090001_identity_core.sql` — `app` helper schema + `set_updated_at`; enums (`account_type`, `platform_role`, `contact_channel`, `user_status`, `public_profile_status`); `users`, `profiles` (incl. server-controlled `public_profile_status`), `contacts`; profile-bootstrap trigger; identity RLS + grants (`primary_account_type` server-controlled).
  - `20260802090002_organizations_tenancy.sql` — enums (`org_status`, `membership_status`); `organizations`, `branches`, `memberships`, `membership_capabilities`, `membership_branch_access`, `platform_role_grants`; tenancy helpers (`current_org_ids`, `is_org_member`, `has_capability`, `current_branch_ids`, `is_platform`); RLS + grants.
  - `20260802090003_audit_foundation.sql` — append-only `audit_log` (immutability trigger; service-role insert; admin read).
  - `20260803090001_verification_and_upgrade.sql` **(Sprint 2)** — enums (`verification_subject`/`verification_type`/`verification_status`); `verifications` + `verification_documents`; internal `app.record_audit_event()`; account-upgrade RPCs (`request_account_upgrade` self-service; `review_start`/`review_request_changes`/`review_reject`/`review_approve`; `apply_account_upgrade`; `set_profile_hidden`); widened audit action allow-list; RLS + grants.
  - `20260803090002_membership_branch_write_paths.sql` **(Sprint 2)** — membership RPCs (`membership_invite`/`activate`/`set_capabilities`/`suspend`/`revoke`, `app.assert_not_last_owner`) + branch RPCs (`branch_assign`/`unassign`).
  - `20260804090001_write_path_security_hardening.sql` **(Sprint 2.1)** — revokes direct privileged/service-role and membership-table DML; verification consistency/immutability/sticky-reviewer/expiry/resubmission guards; stable organization locking; hardened membership/capability/branch RPCs; structural branch tenant trigger; exact ACL reassertion.
- **Public discovery:** curated views `organization_public_directory` / `profile_public_directory` expose only approved columns; base tables are private (Sprint 1.1 B1). Professional discovery requires server-controlled `profiles.public_profile_status='listed'` (Sprint 1.2) — a professional account type alone is not enough; `users.primary_account_type` is server-controlled (not client-updatable).
- **RLS + privileges:** enabled with explicit policies on every table. On the ten reviewed base tables, `anon` has no privilege; `authenticated`/`service_role` have SELECT only, except safe column updates (`users.locale` for both; self-profile display columns for `authenticated`). No application role has direct privileged identity, verification, membership, capability, assignment, platform-role, or audit DML. Covered by **254 pgTAP** assertions and two race scripts in `supabase-rls`.
- **Seed:** synthetic local fixtures (2 orgs, 3 branches, 5 users incl. a branch-limited member and a platform admin) in `supabase/seed.sql` — clearly marked synthetic; no real data.
- **Generated types:** `frontend/src/types/database.types.ts` (from the local schema).
- **Schema source of truth:** `supabase/migrations/*.sql` only (ADR-0002). Design decisions: [ADR-0007](../decisions/ADR-0007-identity-and-tenancy-model.md).

## Engagement state — Notifications, Chat, `message.sent` (2026-08-23)

Branch `feature/engagement-notifications-points-core` — **merged to `main` (PR #35)**, together with the subsequent governance cleanup (PR #36 @ `f975c38`). **Points has no schema and no data**; its contract is now specified in [`points-core.md`](../database/points-core.md) and awaits product review — nothing below changed.

**Migrations (5, all applied and green against a clean `supabase db reset`):**

| Migration | What it adds |
|---|---|
| `20260822090001_notifications_core.sql` | `public.notifications`; recipient-only RLS (`recipient_user_id = auth.uid()`, no org-wide and no platform read path); three indexes; internal `app.notify` / `app.notify_org`; `mark_notification_read` / `mark_all_notifications_read` |
| `20260822090002_notifications_event_wiring.sql` | 15 `app.notify_org` emissions across 13 existing commerce/verification RPCs — no new call sites, each beside an existing `app.record_audit_event` in the same transaction |
| `20260823090001_chat_core.sql` | `public.conversations`, `public.messages`, `public.conversation_read_state`; party-derived RLS; `open_conversation` / `send_message` / `mark_conversation_read`; `app.can_participate`, `app.conversation_parties` (both internal-only) |
| `20260823090002_chat_message_notifications.sql` | `ck_notifications_event_type_known` gains `message.sent`; `send_message` emits one notice to the opposite party |
| `20260823090003_message_sent_no_owner_fallback.sql` | `app.notify_org` gains `p_allow_owner_fallback` (default `true`); `send_message` passes `false` |

**Model, in one line each.** A notification belongs to a **person** — `organization_id` is context and must never reach a `USING` clause. A conversation is a property of a **transaction** — it is anchored to an `rfq`, `quotation` or `order`, both party organizations are derived from that record, and **no RPC accepts an organization id**. Chat access is `active membership + conversation.participate` in one of the two party orgs, so a colleague holding the capability reads the whole thread; there is no participant list and no way to start a conversation with a person.

**`message.sent`** is emitted by `send_message` in the same transaction as the message insert, to the **opposite** party only, via `app.notify_org` with `conversation.participate`. It deep-links to the underlying record, carries `counterparty_name` and **never the message body**, and is the one event with the `org.manage` owner fallback **disabled** — an owner without the capability cannot open the thread, so is never told one exists. Where the counterparty has no holder, nobody is notified and the message still persists. No dedupe, grouping or digest in the Pilot (chat-core.md Q6, decided).

**Application / UI.** Notifications: one `notification-list` serving the header panel and the supply dashboard, real counted badge, `router.refresh()` reconciliation. Chat: caller-scoped query layer (`server/queries/chat.ts`), RPC-only mutation layer (`server/actions/chat.ts`), view models, a bounded recent conversation list and the thread **inside the existing header Chat panel** — there is deliberately **no `/chat` route** — plus one entry point each on the RFQ, quotation and order detail pages. `project` is not a chat subject; a project uses its parent order's conversation.

**Deliberately absent:** Realtime (no table joined `supabase_realtime`), Points, attachments, message edit/delete, reactions, typing, presence, read receipts, notification preferences, and any consumer/personal DM model.

**Verification (2026-08-23).** pgTAP against a clean reset: `31_notifications_core_test` 38/38 · `32_notifications_event_emission_test` 50/50 · `33_chat_core_test` 106/106 · `34_chat_message_notifications_test` 46/46 — **240 assertions, 0 failures**. Frontend targeted suites green; `tsc --noEmit` and `eslint .` clean. Chat UI reviewed in a real browser through the real Email-OTP path against a production build (both parties, send, persistence after reload, unread badge reconciliation, AR RTL + EN LTR, light + dark, 393 px).

> **Scope note.** This section was reconciled on 2026-08-23 for the engagement increment only. Several sections above it (`Implemented routes`, `Auth state`, the `Migrations (7)` inventory, `Deployment state`) predate Sprint 2 and are **known stale** — the repository now has **35 migrations** and **35 pgTAP suites**, and a Vercel project (`aladdin-staging`) is connected and deploying this branch. Refreshing those is a separate pass and was deliberately not attempted here.


## Running services

None running as part of this session. Local dev is manual:

- Frontend dev → `http://localhost:3000`
- Backend API → `http://localhost:8000` (`/health`)
- Supabase local stack → Docker — started, validated (reset ×2 + lint + extension inspection), and **stopped** this session (not left running).

## Deployment state

None provisioned. No Vercel or Supabase cloud project connected, and no CI/CD pipeline. Targets are decided ([ADR-0009](../decisions/ADR-0009-vercel-services-deployment.md) — **Vercel Services** for both `frontend/` and `backend/`, plus Supabase Cloud) and the repository-side definition exists (root `vercel.json`, Node-runtime middleware, `.vercel/` ignored), but nothing has been deployed. Worker host intentionally undecided.

## Environment state

- `frontend/.env.example` and `backend/.env.example` present; **no real `.env` committed** (correct).
- Config is validated per service (`frontend/src/lib/env/`, `backend/app/config.py`); fail-fast in staging/production.
- Toolchain: `uv` at `…/pythoncore-3.14-64/Scripts/uv` (add to PATH); backend uses uv-managed **Python 3.12** (system Python is 3.14). Supabase CLI via `pnpm exec supabase`.

## Current blockers

- **None.** The previously environment-blocked network checks are now **executed and passing** (2026-08-01): the backend Docker image builds/runs (non-root, healthy `/health`), and the Supabase local stack starts, resets (×2), and lints cleanly with the extensions migration applied. See *Infrastructure validation status* below.
- **Residual environment note (not a code defect):** `ghcr.io` still shows intermittent TLS-handshake timeouts that **slow** (no longer block) multi-image pulls; retries succeed. Docker Hub and `public.ecr.aws` are reachable.

## Known warnings (benign)

- Frontend: pnpm peer-dep warning (`unrs-resolver` / `@emnapi`).
- Backend: `StarletteDeprecationWarning` from `fastapi.testclient` under pytest. No functional impact.

## Active files (this session)

- Private visual source: `UI-UX/design.pen` — 8 top-level groups, 207 product screens, 0 generic missing placeholders, 48 explicitly classified remaining gaps, and updated `00I` coverage.
- Approved design record: root `DESIGN.md` — The Aperture concept, exact brand/token values, typography, component defaults, and usage rules.
- Frontend token bridge: `frontend/src/styles/tokens.css`, `frontend/tailwind.config.ts`, `frontend/src/app/globals.css`, and `frontend/src/app/layout.tsx`.
- Product/UI memory: root `PRODUCT.md` and `UI-UX/UI_UX_SYSTEM_GUIDE.md` now record the approved identity and artifact-authority chain.
- Local tooling sidecar: `.impeccable/design.json` is synchronized but remains intentionally ignored; `DESIGN.md` is the versioned durable record.
- Operations memory: `docs/operations/AGENT_WORK_LOG.md` and this file.
- **Design-fidelity harness (not a gate, and not part of runtime rendering):** `frontend/design-lab/` — `session.mjs` caches a signed-in cookie jar so a visual loop does not pay the OTP round trip every run; `shots.spec.ts` photographs the supply dashboard against the approved reference; **`matrix.spec.ts`** captures the account matrix (six identities × EN/AR × light/dark × desktop/phone) into `design-lab-shots/<SHOT_OUT>/`; **`regress.mjs`** diffs two such runs pixel-wise (decoding both PNGs in a browser canvas — deliberately no new image dependency); **`where.mjs`** profiles *where* two shots differ when a percentage alone is ambiguous. Captured shots stay gitignored and are reproducible from the harness. **Trap:** the reduced-motion spec reports a vacuous single-position pass on a cold dev server, because its 900ms sample window closes while Next is still compiling the route — run it twice.

## Design validation status (2026-08-01)

- `design.pen`: 8 top-level groups; 0 top-level overlaps; 0 organizational sibling overlaps.
- 120 original product screens preserved; 87 copied variants added, for 207 product screens total.
- 0 generic `MISSING —` placeholders remain; 48 gaps are explicitly Partial, Blocked, Responsive Decision, Unresolved, or Not Required.
- Known inherited source-screen warnings remain untouched; copied variants reproduce those source conditions where applicable.
- Root `PRODUCT.md` and `UI_UX_SYSTEM_GUIDE.md` encode the permanent governance rule and the approved Aperture identity.
- Frontend primitives and light/dark semantic aliases mirror `DESIGN.md`; Impeccable detector returned 0 findings.
- Semantic normal-size text clears WCAG AA in both themes (minimum measured ratios: light 4.76:1; dark 5.40:1).

## Infrastructure validation status (2026-08-01 — Docker + Supabase gate)

- **Docker — PASSED:** server 29.6.2; `docker build --no-cache -t aladdin-backend-foundation ./backend` ✅. Image: Python **3.12.13**, user **appuser (uid 10001)**, HEALTHCHECK **healthy**; `docker run` → `curl --fail /health` **200 `{"status":"ok","service":"backend","env":"local"}`**; process **uid 10001**. No `.env`/`.pen`/PDF/customer-data/`.git`/app-logs (only base-image `apt/dpkg` logs); **no Alembic, no SQLAlchemy**. Test container removed.
- **Supabase — PASSED:** `start` ✅ (db/kong-API/auth/storage/realtime/studio healthy; imgproxy+pooler disabled in config; `vector` log-router flaps benignly). `db reset` ✅ ×2 (repeatable, no drift). `db lint` ✅ ×2 (all findings in bundled `extensions.*` PostGIS/pgcrypto functions; **zero** in our migration or `public`).
- **Extensions:** pgcrypto 1.3 · pg_trgm 1.6 · vector 0.8.2 · postgis 3.3.7 — all in the `extensions` schema. **0 product tables in `public`.** Applied migration: `20260729000000`.
- **Cleanup:** `supabase stop` ✅; backend test container removed; no repo artifact added. This gate modified **no `.pen` file**; the canonical `design.pen` changed on disk only via a concurrent design-agent flush (gitignored — outside the git tree, no merge impact).

## Validation status (2026-08-01 brand-token extraction)

- **Frontend — GREEN:** TypeScript ✅ · ESLint ✅ · Vitest 3 passed ✅ · Next.js 15.5.22 production build ✅.
- **Generated routes:** `/`, `/_not-found`, `/api/health`.
- **Repository checks:** `git diff --check` ✅; 154 internal Markdown links checked with 0 broken; no dependency or lockfile change.
- **Tooling note:** Codex's pnpm wrapper required a one-time locked dependency relink after its bundled runtime changed; validation ran successfully through the restored local binaries.

## Validation status (2026-07-30 foundation review)

- **Frontend — GREEN (fully re-run):** `pnpm install --frozen-lockfile`, `typecheck`, `lint`, `test` (3 passed), and `build` (production build succeeds; routes `/`, `/_not-found`, `/api/health`).
- **Backend — GREEN (fully re-run):** `uv sync --frozen`, `ruff check` (clean), `pytest` (3 passed). Fail-fast verified (staging + missing secrets → `ValidationError`); `/health` → `200 {"status":"ok"}`.
- **Docker image build — PASSED (2026-08-01):** `docker build --no-cache` ✅; runtime Python 3.12.13, non-root appuser (uid 10001), HEALTHCHECK healthy, `/health` → 200; no `.env`/`.pen`/SQLAlchemy/Alembic in image. See *Infrastructure validation status*.
- **Supabase full stack — PASSED (2026-08-01):** `start` ✅, `db reset` ✅ ×2 (repeatable, no drift), `db lint` ✅ (findings only in bundled `extensions.*`); extensions installed in the `extensions` schema; 0 product tables.

## Phase 1 validation status (2026-08-03 — Sprint 2.1 independent review)

- **Supabase — GREEN:** two fully completed clean cycles — all **7 migrations** + seed apply from clean state; `db lint --schema public,app` has no findings; `supabase test db` is **254/254 PASS** across 14 files on both cycles. Two-session last-owner race PASS (second session blocked ≈2.74–2.80 s; one owner) and conflicting-approval race PASS (second blocked ≈2.70–2.71 s; one immutable approval/audit).
- **Catalog/behavior verified:** 14 workflow RPCs are postgres-owned, volatile, `security definer`, pinned `search_path=""`, executable by `authenticated` only; four internal helpers are postgres-only. Exact table/column grants prove no direct application-role privileged or membership DML. Service-role account-type update and audit spoof insert are denied. Audit failure rolls back account type and `applied_at`. Cross-subject/expired/stale/self approvals are denied; needs-more-info resubmission requires a fresh review claim.
- **Sprint 1.x hardening intact:** default `TRUNCATE` stripped; server-controlled `primary_account_type`/`public_profile_status`; public views expose only approved columns; single-source branch authority.
- **Frontend — GREEN:** frozen install · typecheck · lint · **7 tests** · production build. Server-action modules import `server-only`, preserve the caller client/JWT, use no service-role client, propagate errors, and validate UUID RPC results.
- **Backend — GREEN:** `uv sync --frozen` · `ruff` (clean) · `pytest` (**10 passed**). No backend helper added — the write paths are Next.js server actions, not the FastAPI AI service (ADR-0001).
- **CI:** existing `supabase-rls` workflow (not duplicated) runs reset/lint/254 pgTAP, both race scripts, then repeat reset+pgTAP. Required checks remain `frontend`, `backend`, `docs`, `supabase-rls`.
- **No `.pen` file modified.** No service-role key in client code. No duplicate base profile/identity possible.

## Deferred validation / follow-ups

- JWT custom-claim optimization for the `app` helper functions (ADR-0007 D1; TECHNICAL_DEBT).
- Org-visible audit scope; org-creation anti-abuse cap; storage-bucket creation (deferred to the feature that uploads).
- Platform-role administration remains a reviewed migration/DBA owner transaction until a constrained audited grant/revoke RPC is approved.
- `expires_at` is enforced at apply time; scheduled materialization of status=`expired` awaits the jobs feature.

## Next planned work

**Staging-to-launch completion order, recorded 2026-09-07** — see [`PRODUCT_DIRECTION_GUIDE.md`](../product/PRODUCT_DIRECTION_GUIDE.md#staging-to-launch-completion-order-approved-2026-09-07) for the full 11-item order and the *Admin Control Requirements* backlog convention it establishes. In short: staging readiness (this Phase 0 effort — done) → non-admin account review starting with **Hana Mansour** → cross-account journeys → Landing Page/public surfaces → registration/onboarding/identity/auth → a dedicated auth-and-identity-security hardening pass → a global UI/UX audit → a full application-security audit → remaining product features (incl. Full Chat) → **Platform Admin last** → the final release gate.

**Immediately next: merge `chore/staging-readiness-phase0` (this session's PR), then begin interactive review of Hana Mansour** (`hana-showroom-owner` in [`staging-demo-accounts.md`](staging-demo-accounts.md)) — not Platform Admin, which this order deliberately holds until item 10.

The **Installer/Technician account/persona** milestone is **complete end-to-end** and merged (PR #40) — see the FINAL INSTALLER/TECHNICIAN MILESTONE GATE override above for the full gate detail: Jobs, Work, Portfolio and Certificates, Reviews, Network, Referrals, Points and Settings are all real, RLS-enforced, and gate-verified. **Next Pilot *feature* milestone (item 9 of the completion order, not next in sequence): Full Chat / Realtime** — not started; no `/chat` route, no participant list, no attachments, no Realtime subscription exists for the Installer surface.

> The numbered items below predate Sprint 2 and are **known stale**, like several sections above; they were deliberately not rewritten by this increment.

1. Update PR #4 from `feature/account-upgrade-verification`; require `frontend`, `backend`, `docs`, and `supabase-rls`; do not merge until all four are green.
2. After explicit merge authorization, close Phase 1 documentation/milestone state. Do not begin another sprint from this review task.

## Exact resume commands

```bash
# Re-derive live git state
git branch --show-current
git rev-parse HEAD
git rev-list --count main..HEAD
git status --short

# Backend (uv-managed Python 3.12; ensure uv on PATH)
export PATH="$PATH:/c/Users/h0moh/AppData/Local/Python/pythoncore-3.14-64/Scripts"  # bash
cd backend && uv sync --python 3.12 && uv run ruff check . && uv run pytest && cd ..

# Frontend
pnpm --filter frontend typecheck && pnpm --filter frontend lint && pnpm --filter frontend test

# Supabase (needs Docker)
pnpm exec supabase start && pnpm exec supabase db reset
```
